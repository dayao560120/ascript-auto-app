# 🚀 快速推送到 GitHub

## ✅ 已解决的问题

- ✅ Git 安全目录权限问题已修复
- ✅ 已添加 `C:/` 到 Git 安全目录列表

---

## 📋 操作步骤（3 步完成）

### 步骤 1：在 GitHub 创建仓库

1. **访问**：https://github.com/new
2. **填写**：
   - Repository name: `ascript-auto-app`（或您喜欢的名称）
   - Description: `AScript 自动化控制 APP`
   - Visibility: ✅ Public（公开免费使用 Actions）
   - ❌ **不要勾选** "Initialize this repository with a README"
3. **点击**："Create repository"

---

### 步骤 2：复制并执行推送命令

GitHub 创建完成后会显示以下命令，**复制并在此项目中执行**：

#### 方式 A：HTTPS（需要 Token）

```bash
# 替换为您的仓库地址
git remote add origin https://github.com/你的用户名/ascript-auto-app.git

# 推送
git push -u origin master
```

**首次推送时认证**：
- Username: 您的 GitHub 用户名
- Password: [Personal Access Token](https://github.com/settings/tokens)（不是密码！）

#### 方式 B：SSH（推荐，更简单）

```bash
# 替换为您的仓库地址
git remote add origin git@github.com:你的用户名/ascript-auto-app.git

# 推送
git push -u origin master
```

**首次使用 SSH 需要配置**：
```bash
# 1. 生成密钥（如果没有）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub

# 3. 添加到 GitHub
# 访问 https://github.com/settings/ssh/new
# 粘贴公钥内容
```

---

### 步骤 3：等待自动构建

推送成功后：

1. **进入仓库页面** → 点击 **"Actions"** 标签
2. **查看构建进度**（首次约 30-40 分钟）
3. **下载 APK**：点击完成的运行 → Artifacts → ascript-auto-apk

---

## 💡 常见问题

### Q: 提示 "remote origin already exists"？

```bash
# 删除现有远程仓库
git remote remove origin

# 重新添加
git remote add origin https://github.com/用户名/仓库名.git
```

### Q: 没有权限生成 SSH 密钥？

使用 HTTPS + Personal Access Token 方式：
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选权限：`repo`, `workflow`
4. 生成后复制 token
5. 推送时用作密码

### Q: 推送后 Actions 没有触发？

检查 `.github/workflows/build-apk.yml` 是否存在：
```bash
git ls-files | grep build-apk.yml
```

如果不存在，可能需要重新提交：
```bash
git add .github/workflows/build-apk.yml
git commit -m "Add GitHub Actions workflow"
git push
```

---

## 🎯 最简流程（抄作业版）

假设您的 GitHub 用户名是 `zhangsan`：

```bash
# 1. 关联远程仓库
git remote add origin https://github.com/zhangsan/ascript-auto-app.git

# 2. 推送（会要求输入用户名和 Token）
git push -u origin master

# 3. 完成！去 GitHub 查看构建进度
```

---

## 📞 需要帮助？

如果遇到具体问题，请提供：
1. 使用的推送方式（HTTPS/SSH）
2. 完整的错误信息
3. GitHub 用户名（可选）

祝您推送顺利！🚀
